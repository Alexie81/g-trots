export function reciprocalRankFusion(rankings,k=60,limit=240){const scores=new Map(),sources=new Map();for(const {name,items,weight=1} of rankings)for(let rank=0;rank<items.length;rank++){const id=items[rank].id;scores.set(id,(scores.get(id)||0)+weight/(k+rank+1));if(!sources.has(id))sources.set(id,[]);sources.get(id).push(name);}return [...scores.entries()].sort((a,b)=>b[1]-a[1]).slice(0,limit).map(([id,score])=>({id,score,sources:sources.get(id)||[]}));}
